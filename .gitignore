# Personal / not for the public repo
# (gitignore treats # as a comment only at the start of a line, so keep these
# patterns on their own lines with no trailing inline comments)
plan.md
CLAUDE.md
NOTES.md
scratch/

# Dependencies
node_modules/

# Build / packaging output
dist/
*.zip
*.crx
*.pem

# Secrets (never commit)
.env
.env.local
*credentials*

# Editor / OS noise
.DS_Store
Thumbs.db
*.log
